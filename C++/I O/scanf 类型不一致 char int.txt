int a;
char b;
scanf("%d",&b)会读一个十进制数给b，会造成b的堆栈错误（编译可以通过）；
b=a; 
把int类型变量赋值给char类型的变量，如果小于255，数值正确，如果大于255，则x%256；
printf（"%d",b) 按十进制输出
pirntf（"%c",b)按字符输出

所以scanf一定要类型匹配，想要类型转换读进来再说。
scanf("%d",&a);
b=a;
就可以了。